const achievements = require('../../commands/achievements');
const { assertBasicCommandModule } = require('../helpers/assertBasicCommandModule');

assertBasicCommandModule(achievements, 'achievements');

const insultsCounter = require('../../commands/insults_counter');
const { assertBasicCommandModule } = require('../helpers/assertBasicCommandModule');

assertBasicCommandModule(insultsCounter, 'insults_counter');
